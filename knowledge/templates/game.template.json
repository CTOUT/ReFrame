{
  "_template_version": "1",
  "_instructions": [
    "Copy this file to knowledge/games/<kebab-case-game-name>.json and fill in all fields.",
    "Remove all _instructions and _comment fields before submitting.",
    "Delete optional sections that do not apply rather than leaving them empty.",
    "Verify config paths against a real installation before submitting.",
    "Every recommended value must have a source citation in the top-level sources array."
  ],

  "game": "REQUIRED — exact display name of the game, e.g. 'Dead Island 2'",
  "engine": "REQUIRED — engine name matching the engine field in a knowledge/game-engines/ file, or 'unknown'",
  "engine_version": "OPTIONAL — specific version string if known, e.g. '4.25'",
  "profile_version": "REQUIRED — semantic version of this profile, e.g. '1.0.0'. Increment minor for new keys or corrections, patch for typo/source fixes.",
  "updated": "REQUIRED — ISO 8601 date of last meaningful update, e.g. '2026-04-28'",

  "platforms": [
    {
      "_comment": "One entry per store/platform. Duplicate the block if the game is on multiple stores with different paths.",
      "store": "REQUIRED — e.g. 'Steam', 'Epic Games Store', 'GOG', 'Xbox Game Pass', 'Battle.net'",
      "config_files": [
        {
          "path": "REQUIRED — full path using env var placeholders: %USERPROFILE%, %APPDATA%, %LOCALAPPDATA%, %PROGRAMFILES%, %PROGRAMFILES(X86)%",
          "format": "REQUIRED — one of: ini, json, xml, cfg, txt"
        }
      ]
    }
  ],

  "keys": [
    {
      "_comment": "One entry per config key. Repeat block for each key. Remove _comment field.",
      "key": "REQUIRED — exact key name as it appears in the config file, e.g. 'sg.ShadowQuality'",
      "file": "REQUIRED — filename only, e.g. 'GameUserSettings.ini'",
      "section": "OPTIONAL — section header the key lives under, e.g. '[/Script/Engine.GameUserSettings]'",
      "effect": "REQUIRED — plain English description of what the key controls",
      "recommendations": {
        "performance": "REQUIRED — recommended value for a performance goal",
        "balanced": "REQUIRED — recommended value for a balanced goal",
        "quality": "REQUIRED — recommended value for a quality goal"
      },
      "notes": "OPTIONAL — version caveats, hardware requirements, or other considerations for this specific key"
    }
  ],

  "engine_overrides": [
    {
      "_comment": "OPTIONAL section — only include if this game overrides engine defaults in ways that would cause incorrect recommendations. Remove _comment field.",
      "key": "REQUIRED — the key (or 'sg.*' glob) that is overridden",
      "behaviour": "REQUIRED — describe exactly what the game does differently from the engine default, e.g. 'Reset on launch by in-game menu'",
      "action": "REQUIRED — what ReFrame should do instead, e.g. 'Use in-game slider; direct INI edits will be overwritten'"
    }
  ],

  "manual_only_settings": [
    {
      "_comment": "OPTIONAL section — settings that can only be changed via in-game menus, not config files. Remove _comment field.",
      "setting": "REQUIRED — display name of the setting as shown in the game menu",
      "location": "REQUIRED — menu path, e.g. 'Settings → Graphics → Field of View'",
      "recommendation": "REQUIRED — recommended value or action",
      "reason": "REQUIRED — why this setting matters for the goal or modifier"
    }
  ],

  "intro_skip": [
    {
      "_comment": "OPTIONAL section — one entry per verified method for skipping or disabling intro/splash videos on startup. List methods in preference order (safest and most reversible first). Remove all _comment fields before submitting. Delete this entire section if no skip method is known or verified. %GAME_DIR% in file paths refers to the game's root installation folder (e.g. C:\\Program Files (x86)\\Steam\\steamapps\\common\\<GameName>\\ for Steam).",
      "type": "REQUIRED — one of: file | config | launch_arg",
      "label": "REQUIRED — human-readable description shown to the user, e.g. 'Rename splash video file'",
      "reversible": "REQUIRED — boolean. true if the original state can be fully restored (e.g. rename); false if it cannot (e.g. delete).",
      "platforms": ["OPTIONAL — omit this field entirely if the method works on all platforms. Otherwise list applicable store names, e.g. ['Steam', 'Epic Games Store']."],

      "_comment_file": "The following fields apply when type is 'file'. Remove this comment.",
      "files": [
        {
          "path": "REQUIRED — full path to the target file. Use %GAME_DIR% for install-relative paths or standard env var placeholders (%LOCALAPPDATA% etc.) for user-data paths.",
          "action": "REQUIRED — one of: rename | delete. Prefer rename over delete — it is reversible.",
          "rename_to": "REQUIRED when action=rename — the new filename, e.g. 'intro.bak'. Same directory as the source file."
        }
      ],

      "_comment_config": "The following fields apply when type is 'config'. Remove this comment.",
      "file": "REQUIRED when type=config — filename only, e.g. 'GameUserSettings.ini'",
      "section": "OPTIONAL when type=config — INI section header, e.g. '[/Script/Engine.GameUserSettings]'",
      "key": "REQUIRED when type=config — the exact config key to set",
      "value": "REQUIRED when type=config — the value to write",

      "_comment_launch_arg": "The following fields apply when type is 'launch_arg'. Remove this comment.",
      "argument": "REQUIRED when type=launch_arg — the argument string exactly as it should be entered, e.g. '-novid'",
      "instruction": "REQUIRED when type=launch_arg — platform-specific instructions for applying the argument. Steam: Library → right-click game → Properties → General → Launch Options. EGS: three-dot menu (…) → Manage → Additional Command Line Arguments. Windows shortcut: right-click → Properties → Target field, append after the closing quote with a space.",

      "notes": "OPTIONAL — caveats such as which game update broke this method, EAC/BattlEye interference, or whether the file is recreated on update."
    }
  ],

  "notes": [
    "OPTIONAL — version-specific caveats, known bugs, or other considerations that apply to the game as a whole"
  ],

  "sources": [
    {
      "_comment": "REQUIRED — at least one entry. Remove _comment field. Accepted types: wiki, fix_db, official, community, editorial.",
      "url": "REQUIRED — full URL",
      "type": "REQUIRED — one of: wiki | fix_db | official | community | editorial",
      "label": "REQUIRED — human-readable label, e.g. 'PCGamingWiki — Elden Ring' or 'WSGF — Elden Ring'"
    }
  ]
}
